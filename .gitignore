# Root safety net (lumen/.gitignore already covers node_modules/dist/*.local)
node_modules/
dist/
.env
.env.*
!.env.example

# Local-only spec docs — stay on this machine, never committed, never in history
skills.md
implementations.md
LUMEN_Full_Game_Documentation.md
architecture.md
